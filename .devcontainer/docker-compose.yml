version: '3'
services:
  android-dev:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
      - ~/.gradle:/root/.gradle:cached
    command: sleep infinity
