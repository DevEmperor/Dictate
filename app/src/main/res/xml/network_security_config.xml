<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic so self-hosted local servers can be reached -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
